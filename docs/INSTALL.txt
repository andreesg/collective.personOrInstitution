collective.personOrInstitution Installation
---------------------------------

To install collective.personOrInstitution using zc.buildout and the plone.recipe.zope2instance
recipe to manage your project, you can do this:

* Add ``collective.personOrInstitution`` to the list of eggs to install, e.g.:

    [buildout]
    ...
    eggs =
        ...
        collective.personOrInstitution
       
* Re-run buildout, e.g. with:

    $ ./bin/buildout
